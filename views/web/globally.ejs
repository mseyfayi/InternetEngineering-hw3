<div id="globally" class="d-flex flex-column justify-content-center align-items-center m-3 p-3 border col-9">
    <strong>Globally</strong>
    <div class="d-flex col-6 flex-row justify-content-between">
        <div class="d-flex flex-column mr-3">
            <p class="conf">Cases:</p>
            <p class="rec">Recovered:</p>
            <p class="dth">Deaths:</p>
        </div>
        <div class="d-flex flex-column p-tags-right">
            <p class="conf"><%= all.confirmed %></p>
            <p class="rec"><%= all.recovered %></p>
            <p class="dth"><%= all.deaths %></p>
        </div>
    </div>
</div>